Reference - 
https://serverless.com/blog/make-serverless-graphql-api-using-lambda-dynamodb/

```
    1. Clone the project
    2. run - npm install
    3. Install serverless globally
    4. Set aws cred - 1. serverless config credentials --provider aws --key pastkey --secret pastsecret
    4. To deploy - sls deploy -v
```